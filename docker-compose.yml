version: '3.8'

services:
  credit-risk-api:
    build: .
    container_name: bati-credit-risk-api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - MLFLOW_TRACKING_URI=http://host.docker.internal:5000  
    restart: unless-stopped